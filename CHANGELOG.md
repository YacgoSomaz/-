# 更新日志

本文件记录可供发布和接手者快速阅读的版本级变化。详细开发过程、验证数据和历史背景见 [`DEVELOPMENT_LOG.md`](DEVELOPMENT_LOG.md)。

## [未发布] - 2026-07-20

### 复盘虾主线收口

- 更新器向安装器传递真实安装目录，并显示下载目录、下载进度和安装阶段；覆盖升级不再默认创建第二份目录。
- Inno Setup 从注册表恢复并校验已有安装目录；用户自定义 D 盘路径时仍能覆盖原目录，桌面快捷方式保持指向新文件。
- 完整性清单收窄为启动器与 `app/pipeline/*.pyd` 核心文件，用户素材、导出、模型、本地库和缓存不参与校验。
- 放大查看改用显式打开回调，修复 `nextTick is not a function`；前端运行时错误改为短提示“操作错误，稍后再试”。
- AI 复盘 SSE 强制 UTF-8，修复部分服务返回中文乱码；前端、导出、安装器品牌与图标统一为“复盘虾”。
- 交接报告、快速接手卡、文件地图和排错文档同步到 2026-07-20；本次只处理复盘虾，不修改其他两个客户端。

### 验证与发布边界

- `python -m pytest _experiments\\douyin_worker_route\\tests packaging\\build -q`：`337 passed`，5 个已知弃用 / 依赖警告。
- 本地 `release/LiveWatchSetup_1.1.25.exe`：SHA-256 `a5a6569fdcb42a597b6ace01d4565733d7fde9c951f784ff06784ff89e5b058a`，338,374,466 bytes。
- 该构建尚未自动上传 OSS、签名或替换线上 `update_release`；发布前仍需完成代码签名、对象键、SHA-256、产品码和 `update-v1` 公钥复核。

## [未发布] - 2026-07-16

计划版本：`1.1.15`（线上签名更新通道已经是 `1.1.14`，不得发布更低版本号）。

### 新增

- 官网购买流程改为三页式步骤条：账户概览 → 选择产品 → 确认支付；产品卡增加客户可读的功能介绍，支持上一步返回。
- 官网按客户确认的四段式购买流重排：账户概览、三产品价格、选中产品结算、信任区与精简页脚；下载与帮助改为弹层，联系客服固定展示微信 `cl17733174657`。
- 新增按产品隔离的服务器 SSE 更新通知；运行中的客户端收到签名发布事件后立即重新拉取并验签更新，同时每 60 秒兜底检查一次。
- 强制更新在运行中发现后会阻止业务操作，直到官方安装包完成更新；普通更新仍可稍后安装。
- 正式一键构建改为读取线上发布版本并自动递增最后一段；线上 `1.1.14` 时自动构建 `1.1.15`，不再要求人工输入版本号。
- 产品管理后台可对手机号下单个生效中产品执行“立即停用”，服务端将权益标记过期并记录 `expire` 审计动作。
- 产品管理后台新增“会员授权” Tab，可按已注册手机号选择一至三个产品并延长 1～3650 天；服务端写入 `user_products` 并记录 `admin_product_grants` 审计。
- 新增直播工作台及“实时监控 / 直播效能 / AI直播复盘”二级导航。
- 新增进行中与最近完成直播场次选择，停止后保留话术、互动、时长和历史录像。
- 新增 `recording_sessions` 精确场次边界；旧数据从 `recording_timeline` 推断恢复。
- 新增本机已封口视频预览、横屏 16:9 / 竖屏 9:16 自适应和放大查看。
- 新增手机号账号、受保护本机会话、Ed25519 `account_license` 验签和三产品权益隔离。
- 新增 Ed25519 `update_release` 更新验签、版本 / 域名 / 大小 / SHA-256 校验。
- 新增固定版本 MIT `douyinLive v2.0.24` WSS sidecar，用于弹幕、点赞、进场、礼物、关注和在线统计。
- 新增运营虾违禁词库分级导入，区分风险候选与普通提示词。
- 新增一键商业构建脚本、安装器单实例 / 覆盖升级契约和构建回归测试。

### 修复

- 调整官网单页间距和产品卡尺寸，扩大内容容器、增加卡片留白并放大功能说明，改善局促和拥挤的视觉问题。
- 按反馈撤销滚轮翻页与页面吸附，恢复账户区、产品区、按需结算区和底部保障区的宽松单页布局。
- 修复官网三页流程被错误实现成隐藏面板的问题；现在三个页面完整纵向排列，鼠标滚轮按页吸附并显示明确的滚动提示，避免首屏出现大面积空白。
- 修复后台普通更新留空“最低支持版本”时仍自动填入新版本，导致普通更新被误判为强制更新的问题。
- 修复会员授权页面把手机号正则渲染成 `^1d{10}$`，导致合法 11 位手机号也被前端拒绝的问题。
- 修复商业安装包虽然输入了新版本号，运行时仍回退为 `1.0.0`，从而被线上更新策略误判为已停止支持的问题；页面版本标签现在也只显示运行时返回的真实版本。
- 正式构建新增线上版本预检，必须高于已发布版本才进入 Node / Nuitka 等耗时阶段；版本按语义逐段比较，`1.0.17` 明确低于 `1.1.14`，下一包应使用 `1.1.15+`。
- 修复主播资料被当前抖音登录 Cookie 账号昵称与头像覆盖。
- 修复实时话术混入同主播旧场次数据。
- 修复停止录制后工作台直接清空。
- 修复“全部开始 / 全部停止”按钮未暴露或静默失败。
- 修复未录完的短片段未进入后续转写的问题。
- 修复直播预览横竖屏挤压布局、尺寸过小和放大仍为占位的问题。
- 修复登录前业务按钮先提示“未粘贴链接”而不是要求登录。
- 修复账号区显示内部用户 ID、登录弹窗误触关闭和验证码按钮缺失等界面问题。

### 安全与协议

- 人工会员授权要求管理员 session、角色校验、管理密码二次解锁、同源写请求、服务端产品白名单和审计记录；未登录接口返回 401。
- 权益只信服务端 `user_products` 与验签成功的短时快照；未签名根节点字段不能解锁。
- 账号权益、官网登录交接和支付流程使用不同凭据，客户端不能确认支付成功。
- 更新签名公钥与账号签名公钥分离；两个 key 不能混用。
- 商业构建扫描新增本机会话、Cookie、数据库、AI Key、私钥和旧 WSS vendor 拦截。

### 验证

- 主线完整回归：`296 passed`。
- 仓库级账号 / 构建契约：`43 passed`；本轮合计 `339 passed`。生产 SSE 公网心跳、CORS、非法产品拒绝和普通更新默认值已验证。
- 已生成 `LiveWatchSetup_1.1.15.exe` 候选包：`338291343` 字节，SHA-256 `8806d2c19cb9ba36ed41191a4548bbdfcac929f57e598c70162711a7fd13d651`；安装器、发布清单和编译运行时版本均为 `1.1.15`，完整启动 / 覆盖升级冒烟测试通过。
- 全部房间停止时，真实本地库仍恢复 7 个最近场次；最新场次保留 7 段转写和 369 秒时长。
- 1920×1080、1280×720、1024×768 工作台布局和本机 720×1270 竖屏预览已验证。

### 尚未发布 / 待真实验收

- 主页作品稳定超过 30 条。
- 实际 26 条评论及回复完整采集。
- WSS 互动与音视频流真实直播端到端。
- `1.1.15+` Windows 代码签名、OSS 上传、CDN 校验和签名更新发布。

## [1.1.14] - 2026-07-14

- 当前 `replay_shrimp` 线上签名更新版本。
- 下载对象：`https://download.anyq.site/replay-shrimp/1.1.14/LiveWatchSetup_1.1.14.exe`。
- SHA-256：`4d41794c77049d5e5e84e25a0f731f05fa81fb7bfaf5ef82174fcc3bc65fe133`。
- 字节数：`261169156`。
- 2026-07-15 已验证 `/api/v1/releases/latest?product_id=replay_shrimp` 返回 `update-v1` 签名清单。

## [1.0.15 内部候选，已废弃] - 2026-07-15

- 商业包引入固定哈希的 `douyinLive v2.0.24` WSS sidecar。
- 该包在主播身份保护与直播工作台后续修复之前构建，且版本号低于线上 `1.1.14`；只能作为历史构建记录，不得发布。

## [1.0.12] - 2026-07-14

- 当前正式分发版本，已通过 `download.anyq.site` 提供下载。
- 三产品官网、OSS + CDN + HTTPS 下载和 SHA-256 展示已接通。
- 后续工作区改动尚未进入该安装包。
