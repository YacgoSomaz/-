# 复盘虾开发日志

本文档记录项目从研究验证到商业安装包的主要演进。它用于交接，不替代 Git 提交历史。

## 2026-07-20：账号权益快照缓存防护

- 只读审计确认：复盘虾启动和运行期间均每 60 秒刷新一次 `account_license`，本地授权读取也会重新验证 Ed25519 签名及 `signed_until`；不存在把过期快照继续当会员权益使用的放行逻辑。
- `pipeline/account_client.py` 的 `/api/auth/me` 刷新请求已加入 `Cache-Control: no-cache` 与 `Pragma: no-cache`，避免中间缓存重放过期的短时签名快照。
- 新增账号客户端回归测试，断言刷新请求携带禁缓存头；账号客户端、WebUI 与账号状态测试 `16 passed`。本轮未打包安装包。
- 生产 `anyq.site` 已完成相同部署：`/api/auth/me` 在登录、未登录和受众错误分支均使用 `private, no-store, max-age=0`、`Pragma: no-cache`、`Vary: Cookie, X-Product-Code, X-Device-Hash`，并绕开 Express ETag / 304 语义。部署前备份：`/home/ubuntu/recharge-api/backups/account-snapshot-cache-20260720130347`；远端 `node --check` 与账号缓存 / 版本保留测试 `4 passed`，PM2 `recharge-api` 已重载并通过公网响应头复核。

## 2026-07-17：品牌名称与安装图标统一

- 客户端显示名称、安装器、开始菜单、桌面快捷方式和托盘菜单统一改为“复盘虾”。内部目录、进程名 `LiveWatchLauncher.exe`、更新对象路径和产品码保持不变，避免破坏升级兼容性。
- 后续构建改用 `packaging/build/assets/icon-options/replay-shrimp.ico`：直接由确认版 256×256 原图生成，蓝色渐变底、叠层播放框和播放三角图标；PyInstaller 与 Inno Setup 使用同一图标源。
- 本次只修改源码和打包配置，按要求没有编译或生成新的安装包；现有已安装包的图标和快捷方式要等下一次覆盖安装后才会更新。

## 2026-07-16：复盘虾 1.1.16 商业安装包完成

- 版本守卫根据线上最新版本 `1.1.15` 自动选择下一版本 `1.1.16`，避免误填旧版本导致强制更新。
- Nuitka 业务模块编译、PyInstaller 启动器、Inno Setup 安装器、发布目录安全扫描和完整性清单均通过；安全扫描未发现 Cookie、数据库、音频、日志或开发房间号。
- 安装包：[release/LiveWatchSetup_1.1.16.exe](release/LiveWatchSetup_1.1.16.exe)，大小 `338,281,889` bytes，SHA-256 `22b26c0f9f17a06279fc793cf890267983494fea81451fa60972c394dd0b5dac`。
- 对应发布清单：[release/LiveWatchSetup_1.1.16.release.json](release/LiveWatchSetup_1.1.16.release.json)，对象路径为 `replay-shrimp/1.1.16/LiveWatchSetup_1.1.16.exe`；上传 OSS 后仍需在 `https://anyq.site/admin/releases` 发布签名更新记录，单独上传 EXE 不会触发客户端更新。
- 当前构建产物 Authenticode 状态为 `NotSigned`，Windows SmartScreen 仍可能提示；待代码签名证书可用后应重新签名并重新计算 SHA-256。

## 2026-07-16：官网客户端下载区移到产品价格下方

- 按客户反馈移除首屏品牌小标签、购买说明勾选、产品区辅助说明，以及结算页 `SELECTED PRODUCT` / `ORDER SUMMARY` 英文标签，减少面向客户页面中的内部/技术文案。
- 下载卡片不再以弹窗独立展示；现在位于三款产品价格卡片下方，保留产品名、用途、版本、发布日期和下载按钮。帮助中心仍保留为弹窗，联系客服微信为 `cl17733174657`。
- 线上静态资源缓存版本：`20260716-1518-download-inline`；生产回滚点：`/var/www/recharge-site-backups/20260716151937-downloads-inline`。Nginx 配置检查通过，`https://anyq.site/` 返回 HTTP 200，公网已验证下载区、缓存版本和旧下载弹窗均符合预期。

## 2026-07-16：复盘虾停用会员后及时失效

- `pipeline/webui.py` 新增启动时账号刷新与后台账号刷新线程；默认每 60 秒请求一次 `/api/auth/me`，服务端明确返回 401/403/404/409/410 等停用或失效状态时立即清除本地加密会话和权益快照。
- 网络异常不会误踢用户，但签名权益仍受最多 600 秒的 `signed_until` 约束；账号状态接口也复用同一刷新逻辑，避免“只有手动点击刷新才失效”。
- `pipeline/account_client.py` 为账号错误增加 HTTP 状态与 authoritative 分类，只有明确的服务端拒绝才清缓存；无效签名/畸形权益包也不再保留。
- 回归验证：账号客户端、WebUI、账号签名、更新与构建契约共 `80 passed`；新增停用回归测试先红后绿。随后已完成 `1.1.16` 商业安装包构建。

## 2026-07-16：官网客户文案与首屏排版收口

- 官网首页删除“可用分析能量”等非通用字段，以及退款、自动开通等未经当前协议确认的承诺。
- 首屏标题改为“选择产品，立即开始。”，取消强制断行，避免大字号在不同宽度下错位；产品区改为简短、真实的功能说明。
- 支付页提示改为“付款后同步账户权益 / 有效期按订单计算 / 订单状态可在账户中查看”；信任区同步改为官方购买、权益同步、安装支持。
- 线上静态资源版本：`20260716-1505-copy-cleanup`；已备份到 `/var/www/recharge-site-backups/20260716150401-customer-copy-cleanup`，Nginx 配置检查通过，`https://anyq.site/` 返回 200。

## 2026-07-16：工作区迁移到 D 盘

- 项目实体目录迁移至 `D:\qianshanzimeiti\live_watch`，迁移后文件数、总字节数、Git HEAD 及关键文档/安装包 SHA-256 已校验一致。
- `C:\Users\q2414\Desktop\live_watch` 保留为 Windows Junction，兼容旧脚本与快捷方式；实际数据占用 D 盘空间。
- 已确认并保留现有用户改动，未恢复 `release/LiveWatchPortable_1.0.2.zip` 的删除状态；`D:\live_watch` 为其他旧项目，未操作。

## 2026-07-16：迁移后清理可重建临时产物

- 删除 `staging/_iss_final_verify_163a7e76`、`_iss_singlefile_verify_e9a2ef17`、`_iss_verify`、`_iss_verify_ac86a060` 四个安装验证目录。
- 删除 `.pytest_cache` 与 `.tmp-recharge-api*`、`.tmp-recharge-site` 临时目录；这些内容均可由测试或构建重新生成。
- 保留 `staging/LiveWatch`、`staging/LeadShrimp`、`staging/_leadshrimp_nuitka` 等构建产物，以及 `release/build-1.0.12` 和历史实验目录，供后续 AI 迁移或回滚使用。

## 2026-07-16：官网单页舒展化视觉调整

- 保持普通长页面结构不变，扩大主内容宽度与账户区留白；产品区上下间距、卡片内边距、标题、价格、功能列表和按钮尺寸整体放大，减少局促感。
- 产品卡仍只展示三款产品和面向客户的功能说明，不加入额外技术信息；移动端继续单列并保留可读性。
- 缓存版本更新为 `20260716-1700`，公网验证宽版容器、420px 产品卡留白、滚动吸附关闭均生效。生产备份：`/var/www/recharge-site-backups/20260716140854-spacious-layout`。

## 2026-07-16：回滚滚轮翻页，恢复宽松单页布局

- 根据客户反馈，撤销 CSS 滚动吸附和三页步骤条；官网恢复为普通长页面，账户区、产品区、按需结算区、保障区和页脚自然上下排列。
- 结算区仍保持“选产品后展开”，产品说明继续保留增强版；只放松区块间距和留白，避免首屏或产品卡过度紧凑。
- 页面缓存版本更新为 `20260716-1600`，公网验证滚动吸附关闭、步骤条隐藏、结算区默认隐藏。生产备份：`/var/www/recharge-site-backups/20260716140406-single-page-rollback`。

## 2026-07-16：修正官网滚轮翻页

- 上一版误把三页做成“只显示当前面板”的隐藏切换，打开页面会留下大面积空白，也没有自然的翻页感。
- 现改为三个完整纵向页面同时存在：CSS `scroll-snap-type: y mandatory` + 每页 `scroll-snap-stop: always`，鼠标滚轮向下会吸附到下一页；页面顶部步骤条和页底“向下滚动继续”提示会随滚动更新。
- 第三页之后的信任区和页脚不再单独吸附成第四、第五页。缓存版本更新为 `20260716-1500`，公网验证三面板、滚动提示和滚动吸附规则已生效。生产备份：`/var/www/recharge-site-backups/20260716121739-wheel-pages`。

## 2026-07-16：官网改为三页式购买流程

- 客户确认官网不是一张长页面，而是三页顺序：`01 账户概览`、`02 选择产品`、`03 确认支付`；页面顶部用轻量步骤条切换，产品卡选择后自动进入结算页，支持返回上一步。
- 三个产品卡补充面向客户的功能说明：复盘虾（直播录音与字幕、互动分析、AI 复盘）、漫剧虾（剧本分镜、角色脚本、课程模板）、运营虾（线索采集、直播风险提示、运营增长建议）。价格和权益仍由服务端方案接口决定。
- 生产静态资源缓存版本更新为 `20260716-1400`，公网验证页面状态 200、三页面板各 1 个、结算区无重复 ID。生产备份：`/var/www/recharge-site-backups/20260716114815-three-page-flow`。

## 2026-07-16：官网四段式购买页最终收口

- 按客户确认的四张设计图重排 `anyq.site`：第一段为账户概览，第二段为三产品价格卡，第三段为选中产品后的产品切换与订单支付，第四段为“安心购买 / 立即开通 / 专属支持”及精简页脚。
- 首页不再展示下载卡片、FAQ 或技术校验字段；客户端下载与帮助中心改为点击导航后的辅助弹层，避免干扰购买主流程。下载接口、签名校验和支付接口保持不变。
- “联系客服辅助安装”固定展示客服微信 `cl17733174657`，复制辅助信息只包含客服微信、手机号（若已登录）和安装诉求，不包含 Cookie、验证码或会话令牌。
- 本地静态契约与 JavaScript 语法检查：`10 passed`；公网缓存版本更新为 `20260716-1300`，线上页面验证包含 `checkout-stage`、唯一 `checkoutModal` 和客服微信。生产静态页备份：`/var/www/recharge-site-backups/20260716113826-exact-flow`。

## 2026-07-16：官网客户端下载区客户化精简

- 官网 `https://anyq.site` 的客户端下载卡片已改为面向客户展示：保留产品名、用途、版本、发布日期和下载按钮，移除页面上的 SHA-256 长串及“完整性校验”技术文案。
- 日期来自服务端签名 `update_release.payload.published_at`，不是浏览器本地生成；客户端仍在后台继续校验签名、大小和 SHA-256，安全校验没有降低。
- 已部署官网静态页 `/var/www/recharge-site`，完整页面备份为 `/var/www/recharge-site-backups/20260716030352`，日期格式精修备份为 `/var/www/recharge-site-backups/20260716030531`；静态资源缓存版本更新为 `20260716-1100`，公网已验证新文案和“年/月/日”日期格式生效。

## 2026-07-16：官网三段式客户购买流程重构

- 官网首屏改为“账户概览”：展示手机号账户状态、当前权益、客户端下载入口和购买说明，减少原版首屏的大面积空白。
- 产品区保留三款产品价格货架；点击产品仍进入原有订单确认与微信支付流程，不改变支付、权益或下载接口。
- 页面底部加入“安心购买 / 立即开通 / 专属支持”信任说明和联系客服辅助安装弹窗；辅助信息只包含用户主动复制的手机号与安装诉求，不包含 Cookie、验证码或会话令牌。
- 生产静态页备份：`/var/www/recharge-site-backups/20260716032441`；缓存版本统一为 `20260716-1200`。静态契约与账号测试 `10 passed`，JavaScript 语法检查通过。

## 2026-07-16：运行中客户端实时接收更新与强制停用

- 账号服务新增产品隔离的 `GET /api/v1/releases/events?product_id=...` SSE 通知；发布后台完成签名发布后，只向对应产品的在线客户端推送版本事件。连接带 15 秒心跳、单 IP 连接上限和产品白名单，公网已验证 `200 text/event-stream`，非法产品返回 400。
- SSE 只是不可信的“检查更新触发器”，不携带授权或安装权限。复盘虾收到事件后仍重新请求 `/api/v1/releases/latest`，并按 `update-v1` Ed25519 公钥验证签名、受众、版本、下载域名、文件大小和 SHA-256；伪造事件不能强制安装伪造文件。
- 客户端同时每 60 秒执行一次签名更新兜底检查，解决 SSE 暂时断线、代理重连或漏事件；组件卸载时关闭连接和定时器。
- 强制更新在运行中被发现后会弹出不可关闭的更新提示，并在业务按钮入口阻止继续使用；普通更新仍允许稍后安装。
- 修复发布后台把空白“最低支持版本”一律填成新版本的错误：普通更新默认 `0.0.0`，强制更新才默认等于新版本，避免 `mandatory=false` 仍被客户端判定为强制升级。
- 生产备份：`/home/ubuntu/recharge-api/backups/20260716094507`。服务端新增测试 `8 passed`，公网健康检查与 SSE 心跳通过；复盘虾主线 `296 passed`，仓库级账号 / 构建 `43 passed`，合计 `339 passed`。
- 滚动升级限制：已经安装的旧客户端没有 SSE 与 60 秒轮询代码，无法被服务器凭空长连接推送；必须先发布一版包含本次监听器的安装包。此后用户即使长期不退出软件，也能收到后续发布事件。

## 2026-07-16：修复后台授权后仍显示普通用户

- 生产库核对确认管理后台原本已正确写入 `user_products`，产品 ID、`entitlements_json` 与到期时间均有效；`users.role=regular` 是普通账号的 RBAC 角色，不等于“无会员”，也不应改成管理员角色。
- 根因是官网钱包只把 `/api/auth/me` 的精简 `user` 对象传给渲染函数，又读取已不再返回的旧 `membership_plan / membership_expires_at`，因此无论后台是否开通产品都会显示“基础版 / 暂未开通”。
- 账号服务新增由服务端当前时间和 `products[]` 计算的 `membership` 摘要；只统计真实未过期的 `status=active` 产品，汇总产品 ID、名称、权益和到期时间。该摘要只用于展示，桌面客户端仍只信 Ed25519 签名的 `account_license.products[]` 解锁。
- 官网钱包改为保留完整账号响应并按有效 `products[]` 显示“已开通 N 款”、软件名称和各自到期时间；移除旧字段读取并更新静态资源缓存版本。
- 复盘虾设置页不再把 `account.role` 显示成会员等级；签名产品包含 `replay_shrimp + livewatch` 时显示“复盘虾会员”，否则才显示“普通用户”。
- 生产备份：账号服务 `/home/ubuntu/recharge-api/backups/20260716091423`，官网静态页 `/var/www/recharge-site-backups/20260716091857`。服务端部署快照 `22 passed`、官网账户契约 `2 passed`、仓库主线与构建回归 `338 passed`。

## 2026-07-15：生成并验收 1.1.15 商业安装包

- 自动版本入口读取线上 `1.1.14` 后选择 `1.1.15`，正式构建层再次校验通过；Nuitka 将 `pipeline` 编译为 `pipeline.cp314-win_amd64.pyd`，账号 / 更新双公钥、MIT `douyinLive` sidecar、Node、SenseVoice、声纹模型和本地静态资源均进入白名单产物。
- 构建扫描检查 1767 个文件，未发现 Cookie、数据库、音频、日志或开发房间号；未压缩 staging 为 762.6 MB，Inno Setup 6.7.3 成功生成 `release/LiveWatchSetup_1.1.15.exe`。
- 最终文件为 `338291343` 字节，SHA-256 `8806d2c19cb9ba36ed41191a4548bbdfcac929f57e598c70162711a7fd13d651`；安装器 ProductVersion、发布清单和编译运行时 `LICENSE_APP_VERSION` 均核对为 `1.1.15`。
- 修复冻结商业包冒烟测试仍用 `LIVEWATCH_DATA_DIR` 的旧契约，改为隔离 `%LOCALAPPDATA%`，避免读取真实用户数据；首次启动、首页 200、高级诊断权限保护、模型、日志、数据隔离、覆盖升级保留和升级后重启全部通过。仓库完整回归 `337 passed`。
- 当前安装包尚未进行 Windows Authenticode 代码签名，状态为 `NotSigned`；Nuitka 商业编译、应用完整性签名与账号 / 更新 Ed25519 验签均已启用。上传 OSS 与后台签名发布尚未执行。

## 2026-07-15：构建版本自动递增与会员权益停用

- `一键打包复盘虾.bat` 不再要求手输版本号；交互层调用 `version_guard.ps1 -NextVersion` 读取线上 `replay_shrimp` 版本并自动递增最后一段，当前线上 `1.1.14` 会自动选择 `1.1.15`。正式构建仍再次校验新版本必须严格高于线上版本，避免并发或缓存导致倒退。
- 修复产品管理后台模板字符串吞掉手机号正则反斜杠的问题：渲染结果原为 `^1d{10}$`，现已稳定输出 `^1\\d{10}$`；生产页面与回归测试均验证合法 11 位手机号可通过前端校验。
- 会员授权列表对每个生效中产品新增“立即停用”。新增受管理员 session、角色、短时二次解锁和同源校验保护的 `POST /api/v1/admin/memberships/expire`；服务端只接受手机号和产品白名单，参数化更新 `user_products.expires_at`，并在 `admin_product_grants.action=expire` 中保留停用前后的到期时间。
- 停用在数据库立即生效；客户端已签发的 `account_license` 最长保留约 600 秒，重新登录或刷新权益可立即获取过期状态。生产备份位于 `/home/ubuntu/recharge-api/backups/20260715215938`。
- 验证：账户服务部署快照 `20 passed`，仓库级完整回归 `336 passed`；公网后台包含正确手机号正则、停用入口与 TTL 提示，未登录停用接口返回 401，服务健康检查正常。

## 2026-07-15：修复安装包版本固化为 1.0.0 与构建版本倒退

- 根因一：正式构建已经把输入版本写入生成的 `license_runtime.py`，但 `pipeline/config.py` 只读取进程环境变量，安装后没有该变量便回退为 `1.0.0`；因此即使安装包文件名版本更高，更新器仍会报告“当前版本 1.0.0 已停止支持”。现已优先读取编译进包的 `LICENSE_APP_VERSION`，环境变量仅作为开发 / 测试覆盖。
- 根因二：版本逐段比较，`1.0.17` 的次版本为 `0`，低于线上 `1.1.14` 的次版本 `1`，不能作为新版本。正式构建现在会在 Node、Nuitka 和 Inno Setup 之前读取线上 `replay_shrimp` 发布版本，并拒绝小于或等于线上版本的输入；下一次有效版本为 `1.1.15+`。
- 前端底部和系统设置移除硬编码 `v1.0.0`，统一展示更新状态接口返回的运行时版本；未加载时显示 `—`，避免再次用假版本误导排障。
- 新增 `test_build_version_runtime.py`、`test_version_guard.py` 和前端契约断言，覆盖编译版本注入、版本倒退 / 相等拒绝、合法升级放行及可见版本标签。
- 仓库级主线、旧授权兼容服务与构建契约完整回归 `335 passed`，`git diff --check` 通过。

## 2026-07-15：发布后台增加会员授权 Tab

- `https://anyq.site/admin/releases` 升级为“产品管理后台”，保留版本发布并新增“会员授权” Tab；可输入已注册手机号、选择复盘虾 / 漫剧虾 / 运营虾并设置 1～3650 天。
- 有效权益从原到期日顺延；已过期或未开通从服务器时间起算。产品名称、金额快照和权益完全取服务端 `PAYMENT_PLANS`，前端不能提交任意 entitlement。
- 新增 `GET /api/v1/admin/memberships` 与 `POST /api/v1/admin/memberships/grant`；继续要求管理员登录和短时管理密码解锁，写请求额外执行同源校验。
- 新增 `admin_product_grants` 审计表，记录管理员、目标用户、产品、天数和授权前后到期时间；人工开通不创建支付成功订单。
- 本地后台测试 `17 passed`；生产健康检查、页面 Tab、未登录 GET/POST 401 均验证通过。部署前文件备份位于 `/home/ubuntu/recharge-api/backups/20260715211315`。

## 2026-07-15：仓库交接资料、版本事实与故障索引同步

- 重写根 `README.md`，移除旧卡密主线、`license.runmo.art` 和 `1.0.9/1.0.12` 作为当前版本的误导，改为手机号账号、签名权益、签名更新、WSS sidecar 和当前主线架构。
- 新增 `CHANGELOG.md`、`docs/FILE_MAP.md`、`docs/TROUBLESHOOTING.md`，分别提供版本摘要、逐模块职责和“现象 → 文件 → 数据 → 测试”的故障定位入口。
- 2026-07-15 在线核对三产品更新接口均返回 HTTP 200 与 `update-v1` 签名信封；复盘虾线上版本为 `1.1.14`，SHA-256 `4d41794c77049d5e5e84e25a0f731f05fa81fb7bfaf5ef82174fcc3bc65fe133`，下一次构建修正为 `1.1.15+`。
- `.tmp-recharge-*`、独立 `lead_shrimp` 和构建日志已加入忽略范围，不随复盘虾推送；安装包、Cookie、session、数据库和密钥继续禁止入库。
- 最终验证：主线、旧授权兼容服务和构建 / 安装器契约合计 `331 passed`；Markdown 相对链接和 `git diff --check` 通过。

## 2026-07-15：停止录制后保留直播工作台场次

- 修复停止房间后“实时监控”直接变成空白、刚录完的话术和互动记录无法继续查看的问题。工作台下拉框改为“选择直播场次”，正在录制场次优先，其后提供最近完成的历史场次。
- 新增 `recording_sessions` 精确记录录制开始 / 结束边界；重复的停止或下播信号采用幂等更新。升级前已有录制没有该表记录时，会从 `recording_timeline` 的封口片段按间隔推断场次，不丢旧数据。
- 话术、互动事件、高频问题、在线数与时长均按所选场次的起止时间读取；正在录制和历史场次使用独立 `session_id`，不会把同一主播的两次直播混到一起。
- 历史录像预览按场次时间窗选择本机已封口 MP4；无录像时仍保留话术和互动记录，并明确显示“已结束 / 无可用录像”，不再伪装为正在录制。
- 真实本地库验收：全部房间停止时仍恢复 7 个最近场次，默认最新场次保留 7 段转写和 369 秒时长；主线完整回归 `292 passed`，`git diff --check` 通过。

## 2026-07-15：直播预览横竖屏自适应与工作台响应式布局

- 直播预览会从视频 `loadedmetadata` 自动判断横屏 / 竖屏，在卡片顶部明确显示“横屏 16:9”或“竖屏 9:16”。两种比例均使用完整画面展示，不拉伸、不裁切，也不会因比例变化重排话术和诊断区域。
- 桌面宽屏采用稳定三栏：预览列限制在 320～360px，实时话术占主列，诊断占辅助列；中等宽度保留预览 + 话术两列并把诊断下移；窄屏自动单列，预览最大 420×260，避免横向滚动。
- 预览卡由原 250×140 提升为最高 360×230，并将“预览说明”改成“放大查看”。放大弹窗现在播放真实本机视频段，最高使用 94vw / 72vh，横竖屏继续按原比例居中显示。
- 本机真实竖屏视频 720×1270 验收通过；1920×1080、1280×720、1024×768 三档布局无横向溢出，浏览器控制台无错误。完整主线回归仍为 `288 passed`。

## 2026-07-15：实时话术按录制场次隔离、批量控制修复

- 修复直播工作台把同一主播历史场次转写和互动带入当前“实时话术”的问题。转写按当前房间 `recording_since`（秒）过滤，互动事件按同一起点换算为毫秒过滤；当前场次起点缺失时返回空数据，绝不回退读取历史。
- 修复“全部停止”虽已定义却未暴露给 Vue 模板、点击无效的问题；“全部开始 / 全部停止”现在都有执行中状态、明确执行数量、无可操作房间提示，以及权限或服务端失败原因。
- 批量控制 API 统一返回明确的 `ok` 与变更数量，避免前端把 403/500 静默吞掉后仅刷新页面。
- 新增场次隔离、零起点保护、批量控制 API 与前端暴露契约回归；完整主线回归 `288 passed`。

## 2026-07-15：工作台复用本机视频段预览

- 新增 `pipeline/video_preview.py` 和 `/api/live-preview/{rid}`。接口仅允许当前 `recording` 且已开启“录制视频”的房间读取自己的视频目录，并拒绝路径穿越和正在写入的文件。
- 实时监控不再只显示占位图：会自动播放最新已封口的本机 MP4 视频段；视频结束后自动请求下一段。不会新增第二条平台拉流或打开额外抖音页面。
- 因现有录制为 60 秒 MP4 分段，首个可播画面需要等待第一段封口，预览最大约 60 秒延迟。前端会明确显示“正在生成第一段预览”或“未开启录制视频”，不得伪装成零延迟直播。
- 验证：视频路径 / 稳定文件 / WebUI 接口测试与完整主线回归 `285 passed`。

## 2026-07-15：直播工作台接入真实录制房间

- 新增只读 `GET /api/live-workbench` 与 `pipeline/live_workbench.py`：只读取已有的房间状态、转写 SQLite 与互动事件 SQLite；不额外打开抖音页面，也不会自动调用 AI。
- 工作台顶部增加“正在录制直播间”下拉框，严格只列出 `phase=recording` 的房间。切换选择后，转写、在线人数、每分钟弹幕、录制时长、累计事件和高频问句均限定到该房间，避免跨直播间串数据。
- 没有录制中的房间时展示明确空状态；原有模拟话术、模拟在线数、模拟风险建议已移除。AI 节奏建议仍默认关闭，开启后当前也只提示“待配置触发规则”，不会偷跑 Token。
- 新增数据快照和 WebUI 接口回归；完整主线回归提升至 `282 passed`。

## 2026-07-15：直播工作台导航层级校正

- 左侧“直播工作台”改为纯分组入口：点击只展开或收起二级导航，不再把一级名称误呈现为独立功能页。
- 固定二级项为“实时监控”“直播效能”“AI直播复盘”；分别仍使用既有 `liveConsole`、`pre`、`review` 视图和数据链路。
- 子页面被选中时分组保持展开，父级仍显示选中态；补充前端契约测试。
- 验证：前端契约 `23 passed`，主线完整回归 `279 passed`。

## 2026-07-15：主播身份写入保护修复

- 修复“解析时是目标主播、加入列表后变成当前抖音登录账号”的身份污染：直播 WSS/页面返回的昵称和头像不再无条件覆盖已解析主播资料。
- 已解析的主播昵称是身份锚点；仅当连接返回的昵称与其一致时才允许补充头像。昵称不一致时整组昵称/头像更新直接丢弃，避免把 Cookie 所属账号头像配到目标主播名下。
- 新增回归测试，覆盖“目标主播 + 登录 Cookie 账号”冲突与“空资料可正常补全”两种情形；主线回归 `279 passed`。

## 2026-07-15：恢复正式 WSS 互动采集并构建 1.0.15

- 用户确认弹幕、点赞、进场、礼物、关注和在线统计与音视频流同属复盘虾核心功能；不能因移除旧 vendor 而默认缺失。
- 未恢复旧的许可不明确 `DouyinLiveWebFetcher` / `run_worker.py`。改为固定随包 `jwwsjlm/douyinLive v2.0.24` Windows amd64 sidecar（MIT）；引入归档和二进制均已做 SHA-256 固定校验，MIT 文本随包放在 `LICENSE.douyinLive.txt`。
- 商业启动器检测到 `app/sidecar/douyinLive.exe` 时默认设置 `LIVEWATCH_DANMU_BACKEND=sidecar`；房间启动前复用已验证的本机抖音 Cookie 生成仅本机可读的 sidecar 配置并启动 `127.0.0.1:1088` 服务。Cookie 不进入安装目录、日志、完整性清单或发布包源码。
- sidecar 将 `WebcastChatMessage`、`WebcastLikeMessage`、`WebcastMemberMessage`、礼物、关注、粉丝团和房间统计写入现有 `SqliteSink`；音视频流、录制、转写与 AI 复盘链路保持不变。
- 本机验证：上游二进制 `--version` 为 `v2.0.24/79453ece4a44`；本机 sidecar 启动和 1088 端口冒烟通过；主线回归 `266 passed`；发布扫描通过。
- 已生成 `release/LiveWatchSetup_1.0.15.exe`（SHA-256 `9e3e8bd019e2730df90a304251ae9cdee595a7c63f06e2571ea8d4e384547cd3`，322.60 MB）。该包尚未 Windows 代码签名、上传 OSS、在更新后台签名发布，也尚未以真实直播间完成弹幕/点赞/进场/在线数端到端验收；在这四项完成前不得宣传为最终正式包。

## 2026-07-14：官网分发、三端发布与交接内容校正

- 复盘虾、漫剧虾、运营虾三个安装包已接入 `download.anyq.site` 的 OSS + CDN + HTTPS 分发，官网 `https://anyq.site` 已开放三端下载；客户页面展示版本与发布日期，SHA-256 只用于后台和客户端完整性校验。
- 公网已验证三个安装包均返回 HTTP 200、正确的内容长度和 `Accept-Ranges`；发布流程固定为“本地算 SHA-256 -> 新版本对象路径上传 -> CDN 验证 -> 官网更新 -> 再次公网验证”。
- 为测试手机号 `17733174657` 通过管理端直接开通三个年度产品权益，分别为 `replay_shrimp/livewatch`、`comic_shrimp/comic_course`、`operation_shrimp/operation_course`，有效期至 2027-07-14；未伪造支付订单。
- 更正交接文档：当前主线测试为 `255 passed`；复盘虾正式包为 `1.0.12`，不再使用旧的卡密下载地址或 `1.0.9` 安装包信息。
- 采集功能仍有真实验收缺口：主页可能仅 21～22 条作品，约 26 条评论的视频可能只采到约 11 条。评论按平台 `comment_id` 去重，不能按文案或昵称去重；回复和话术必须保留。

## 2026-07-14：本次接手累计改动索引

- 账号：以手机号登录替换卡密入口，新增本机受保护 session、Ed25519 `account_license` 验签、`replay_shrimp + livewatch` 产品隔离和一次性官网续费交接。
- 抖音数据：统一直播 / 短视频 / 获客 Cookie；主页作品增量读取、缓存合并、评论网络响应、顶级 / 回复分页、回复展开和 `comment_id` 去重均已补充。
- 前端：手机号登录弹窗、验证码按钮、登录前拦截、无权益提示、隐藏内部 ID、评论采集计数展示方向已更新。
- 发布：账号版 Nuitka / PyInstaller / Inno 构建、完整性签名、session 扫描、三产品官网、OSS + CDN + HTTPS 下载已完成。
- 完整的文件级改动、已部署状态、未验收项和下一步顺序统一记录在 `PROJECT_HANDOFF.md` 的“0.2 本次接手累计变更清单”。

## 2026-07-14：手机号账号、账户中心与安全续费跳转

- 客户端授权入口从卡密迁移为手机号短信登录；远端账号服务负责短信校验、用户角色、会员等级、到期时间与权益，客户端不保存短信或支付密钥。
- 本地仅以系统保护的加密文件保存远端会话；`/api/account/*` 不会向前端返回 Cookie 或会话令牌。
- 增加“续费/账户中心”：客户端向远端签发 60 秒、单次使用的交接凭据，网页从 URL 片段读取后立即清除，再换取远端 `HttpOnly` 会话。不得把 Cookie、会话令牌或手机号验证码放到 URL。
- 远端新增 `web_handoffs` 记录，仅保存凭据 SHA-256 摘要；消费为事务内的单次更新，过期/重复使用一律失败。账户中心继续复用 `https://anyq.site` 的充值官网，并显示已登录账户的会员信息。
- 本地设置页显示账号、会员等级和到期时间；本地退出会尝试注销远端会话，即使网络失败也清除本机加密会话。
- 远端健康检查、网页路由、无效交接凭据拒绝均已验证；本地服务已重启。真实“签发 → 消费 → 重放失败”需先在客户端完成手机号登录后再测（当前本机账号状态为未登录）。
- 当日回归：`243 passed`；后续同日完整主线回归已提升至 `255 passed`，见本日志首条校正记录。

## 2026-07-14：统一三产品订单与权益

- 远端付款页的套餐目录改为：复盘虾 + 运营杀招教程（2499 元/年）、漫剧虾 + 漫剧精品课程（799 元/年）、运营虾（799 元/年）。
- 新增远端 `user_products` 表；产品到期、价格、周期、产品权益按 `user_id + product_id` 独立保存，续费不再覆盖其他产品。
- 支付订单保存产品权益快照；支付成功只更新对应产品。
- `GET /api/pay/plans`、登录和 `/api/auth/me` 使用统一 `products` 数组返回产品状态。
- 在隔离 SQLite 数据库完成“同一用户购买复盘虾与漫剧虾”的结算验证：两条权益独立存在，只有复盘虾发放本软件的功能权益。
- 正式契约：`docs/ACCOUNT_PRODUCT_CONTRACT.md`。

## 2026-07-14：冻结权益签名与支付安全协议 v1

- `docs/ACCOUNT_PRODUCT_CONTRACT.md` 升级为三产品共用的冻结协议：服务端 `user_products` 为唯一权益来源，客户端仅消费 Ed25519 签名且短时有效的 `account_license`。
- 明确区分 session、权益签名、网页交接票据和支付 nonce；签名快照不能登录、不能支付确认，高价值云端功能仍须服务端实时二次鉴权。
- 冻结产品 ID、支付回调和服务端授权规则；客户端团队只能实现验签和产品隔离，远端账号服务负责人唯一可改订单、回调、密钥和数据库。
- 初始远端签名实现曾缺 `key_id` 等协议字段；客户端传入的产品码、设备 hash 均不得作为授权依据，后续已按下一条记录修正。

## 2026-07-14：复盘虾签名权益适配完成

- 远端 `account_license` 已补齐 `schema`、`key_id`、`iss`、`aud` 和 10 分钟上限；产品受众只取白名单 `X-Product-Code`，不再信任请求体产品码或设备 hash。
- 校正远端公开验签密钥与实际 Ed25519 私钥的配对关系；私钥仍只存在远端环境，复盘虾仅内置公钥。
- 复盘虾新增严格验签器：原始签名字节验证后才解析权益；篡改产品、过期、未知 key、错误受众或跨产品权益一律不能解锁。
- 本地旧加密会话可无感换取新签名，但旧数据本身不能再作为授权；账户状态每分钟经受保护 session 刷新签名快照，Cookie 不返回给前端。
- 回归：账号相关 18 项、完整测试 251 项通过；真实手机号端到端验收与另外两个客户端的同协议适配仍待完成。

## 2026-07-13：授权后台与商业交接文档整理

- 线上授权管理台已重新部署，入口仍为 `https://license.runmo.art/admin`。
- 授权管理台产品名改为「直播复盘侠 · 授权管理台」。
- 移除管理台中的「卡密前缀」「干净授权」「解绑」等容易误导或不再需要的前端项。
- 新增卡密删除能力；删除后卡密不再显示，已绑定设备会在下一次授权刷新后失效。
- 新增管理员 24 小时信任会话：同一 IP、同一浏览器设备指纹、同一信任 Cookie 命中时，不需要反复输入管理员令牌。
- 保留卡密有效期下拉：1 分钟、一周、一个月、半年、一年，便于测试过期授权。
- 客户端授权刷新默认值核准为 600 秒，即约 10 分钟一次。
- 本地仓库已同步线上授权后台改动，并推送提交 `c78f543 Improve licensing admin and commercial integrity checks`。
- 授权核心回归测试通过：`36 passed`。
- 重写 `PROJECT_HANDOFF.md`，补充产品功能、系统架构、目录地图、授权服务、打包发布、已知问题和下一步优先级，方便下一个 AI 或工程师接手。
- 清理仓库根目录与 `_experiments`：根目录早期原型移入 `archive/legacy_root_prototype/`；删除旧 `dycast`、`dycast_original_route_161517`、`douyin_live_run`、视频号等废弃实验路线。
- `_experiments` 现在只保留当前主线 `douyin_worker_route`、SenseVoice 模型目录和声纹模型目录；运行期 DB、日志、音频、导出、Cookie、短视频缓存均按本地数据处理，不作为源码上下文。

## 2026-06：直播监听与录音链路

- 建立抖音直播监听原型，验证弹幕、进场、点赞、关注等事件入库。
- 尝试 dycast 前端批量托管路线，后因高频页面访问与风控风险，逐步迁移到后端 worker 思路。
- 搭建音频取流、ffmpeg 录音、SenseVoice 转写流程。
- 发现“多个直播轮流录一分钟”会导致大量音频空窗，改成每个房间独立录制。
- 对 segment muxer、单段录制、断流、短段、空段进行多轮实测，最终形成“保留录音台账，不制造账乱”的方向。
- 增加录音时间轴、片段状态、导出溯源能力。

## 2026-06 下旬：产品化前端与导出

- 前端从早期暗色测试台逐步改成类商业软件的左侧导航、数据大盘、AI复盘、效能分析等页面。
- 添加主播资料解析、头像缓存、本地数据清理、单房间导出、汇总导出。
- 声纹识别接入导出表，支持“发言人A/B/C”标注。
- 增加累计观看、峰值在线、弹幕、点赞、进场等指标解释，避免误导用户。

## 2026-07 初：AI 复盘与效能分析

- 接入 DeepSeek/OpenAI 兼容接口，支持直播复盘报告、追问上下文和报告下载。
- 复盘报告从纯文本逐步优化为摘要、卡片、图表和 HTML 报告。
- 增加 AI 处理进度动画、事件流、报告生成状态提示。
- 效能分析从固定 Python 权重逐步转向“数据指标 + AI 判断”的混合方式。
- 引入敏感词库，用于风险提示，不作为重度扣分项。

## 2026-07：短视频中心

- 新增短视频中心 Tab，支持粘贴抖音主页链接解析账号和作品。
- 作品卡片展示封面、标题、点赞数、选中状态，并支持加入 AI 工作台。
- 接入 `XBuilderLAB/cheat-on-content` 的 MIT 方法论作为短视频评分、预测、对标学习和发布后复盘参考。
- 在 `pipeline/short_video_ai.py` 中包装为用户可见的“作品潜力分、爆款预测、关键问题、优化建议”。
- 支持下载逐字稿和 Markdown 报告。
- 当前已知问题：主页作品解析仍可能只能拿到初始一屏，需继续完善模拟滚动和增量加载。

## 2026-07：AI 获客系统

- 接入评论线索适配层，新增“AI获客系统”页面。
- 支持解析主页作品、选择视频、采集评论、去重入库。
- 线索表展示评论人、评论内容、IP属地、时间和“去联系”入口。
- 产品方向从“单视频评论采集”调整为“监控竞品主页新作品评论”，后续需加入定时监控和 AI 清洗。

## 2026-07：合规与第三方依赖整理

- 移除旧的未明确商用授权 WSS vendor，不再把旧 `DouyinLiveWebFetcher` 路线作为默认内核。
- 默认后端改为 `audio_only`，可选外部 MIT sidecar 通过 JSON WebSocket 事件接入。
- 新增 `THIRD_PARTY_NOTICES.md`，登记 Vue、Element Plus、cheat-on-content、SenseVoice、3D-Speaker、Silero VAD 等来源与分发边界。
- 构建扫描加入旧 vendor、cookie、数据库、日志、录音、AI Key 等拦截。

## 2026-07：商业授权与安装包

- 新增本地授权模块：
  - `license_manager.py`
  - `license_client.py`
  - `license_policy.py`
  - `license_refresh.py`
  - `license_clock.py`
- 新增授权服务器 `licensing_server/`：
  - 卡密生成
  - 设备绑定
  - Ed25519 授权签名
  - 授权刷新
  - 冻结/解绑
  - 管理后台 `/admin`
- 商业安装包构建：
  - `packaging/build/build_release.ps1`
  - `livewatch_launcher.py`
  - `livewatch.iss`
  - `check_release.ps1`
  - `check_release.py`
- 商业构建通过 Nuitka 编译 `pipeline`，安装包不携带业务 `.py` 源码。
- 默认安装路径改为 `C:\Program Files\LiveWatch`，窗口默认最大化，安装包加入图标。
- 修复授权后台根路径：`https://license.runmo.art/` 会自动跳转到 `/admin`。
- 修复卡密过期和后台冻结仍可继续使用的问题：
  - 服务端显式过期卡密不再额外叠加离线宽限期，1 分钟测试卡会按 1 分钟真实过期。
  - 客户端把服务器明确拒绝（过期、冻结、禁用）与普通网络失败区分开。
  - 服务器拒绝授权刷新时，客户端会清除本地授权缓存，避免继续靠旧缓存使用。
  - 商业包授权刷新间隔从长周期改为约 60 秒，启动时也会主动刷新。
  - 新商业安装包：`release/LiveWatchSetup_1.0.9.exe`。

## 当前线上状态

- 授权服务域名：`https://license.runmo.art/`
- 管理后台：`https://license.runmo.art/admin`
- 服务进程：`livewatch-license.service`
- 运行端口：本机 `127.0.0.1:60001`，由 nginx 反代到 HTTPS。
- 2026-07-11 已部署授权过期/冻结修复版服务端。
- 历史卡密路线最后记录的商业安装包版本：`1.0.9`。当前正式账号版复盘虾安装包为 `1.0.12`，下载地址与 SHA-256 以本日志首条和 `PROJECT_HANDOFF.md` 为准。

## 当前主要技术债

- 短视频账号解析的“继续加载更多作品”还不稳定，需优先修。
- AI 复盘报告仍需继续提升图文排版、可读性和下载格式。
- AI 获客系统需要从“手动视频评论采集”升级为“竞品主页新作品定时监控”。
- 代码签名证书还未接入，安装包仍会显示未签名。
- 商业授权后台只有单管理员令牌，后续可加多账号、操作审计和导出。

## 2026-07-17：OSS 旧安装包自动清理

- 线上 `/home/ubuntu/recharge-api` 增加 `release-retention.js`：按每个产品的语义化版本排序，保留最新 3 个已发布版本。
- 超出保留窗口的已发布/已撤回安装包先进入 7 天回收期；只在回收期结束且再次确认不属于最新 3 个版本时，通过服务端 OSS 签名 DELETE 删除对象。
- 当前版本、草稿版本和回收期间重新回到保留窗口的版本不会被删除；长期不发布新版本不会触发删除。
- 清理任务启动后 10 秒执行一次，随后每 6 小时执行；失败任务记录错误并每小时重试。
- 管理后台版本发布页新增“旧版本自动清理”状态卡和手动检查按钮；所有任务写入 `release_retention_jobs`，可审计待清理、已清理和失败重试记录。
- 首次部署已发现并排队 3 个候选对象（复盘虾 1.1.14、复盘虾已撤回 0.1.21、运营虾 0.1.13），`not_before` 为 2026-07-24；本次部署没有立即删除 OSS 文件。
- 线上备份目录：`/home/ubuntu/recharge-api/backups/retention-20260717011800`；远端 `node --check` 与 2 个保留策略测试通过，PM2 `recharge-api` 健康检查正常。

## 2026-07-20：安装目录、完整性范围、预览错误与品牌收口

本次只处理复盘虾主线，不修改漫剧虾或运营虾仓库。变更集中在主线业务、Windows 商业构建和交接文档：

- 更新器：`pipeline/updater.py` 在下载状态中返回实际安装目录，启动安装器时传入带引号的 `/DIR="安装目录"`；`packaging/build/livewatch.iss` 通过注册表 `Inno Setup: App Path` 恢复并校验原路径，解决用户改装到 D 盘后更新落到第二目录、桌面快捷方式仍启动旧包的问题。
- 更新体验：前端更新弹窗显示安装目录、当前下载阶段、字节进度和安装提示；普通更新与强制更新沿用签名 `update-v1` 清单，不能把 OSS 文件列表当更新源。
- 完整性：`pipeline/integrity_manifest.py` 的 allowlist 收窄为启动器和 `app/pipeline/*.pyd` 核心文件。用户素材、导出、模型、数据库、缓存和保存路径的正常变化不再触发完整性拒绝。
- 运行时错误：`pipeline/frontend.html` 将放大预览改为显式 `onPreviewDialogOpen`，修复 Vue 运行时 `nextTick is not a function`；全局异常改为记录诊断并显示短提示“操作错误，稍后再试”，不再渲染整屏原始堆栈。
- AI 复盘：`pipeline/ai_report.py` 对 SSE 响应显式设置 UTF-8，修复服务端未声明 charset 时的中文乱码；导出、效能分析和 Web UI 文案同步使用“复盘虾”。
- 发布资源：安装器品牌和图标统一为“复盘虾”，图标源为 `packaging/build/assets/icon-options/replay-shrimp.ico`；发布包仍需另行完成 Windows 代码签名、OSS 上传和后台签名清单发布。

验证命令：

```powershell
$env:PYTHONPATH="$PWD\_experiments\douyin_worker_route"
python -m pytest _experiments\douyin_worker_route\tests packaging\build -q
```

结果：`337 passed`，5 个已知弃用 / 依赖警告。当前本地构建 `release/LiveWatchSetup_1.1.25.exe` 的 SHA-256 为 `a5a6569fdcb42a597b6ace01d4565733d7fde9c951f784ff06784ff89e5b058a`，大小 `338,374,466` bytes；本次只推送源码和文档，不自动上传 OSS。

## 2026-07-20：AI 复盘页面导航收口

- AI 复盘页将“AI复盘报告”和“AI专场顾问”拆为两个 tab，默认进入报告，追问功能通过第二个 tab 聚焦展示，避免两个长面板同时占满页面。
- 直播工作台侧边栏子项调整为“实时监控 → AI直播复盘 → AI达人雷达”；原“直播效能”用户可见文案统一改为“AI达人雷达”。短视频中心和 AI 获客系统继续位于该分组之后。
- 仅改动复盘虾 `pipeline/frontend.html` 与前端契约测试，未修改漫剧虾、运营虾，也未打安装包。
- 相关测试：`test_frontend_contract.py` 29 项通过；提交前继续执行主线与 `packaging/build` 全量回归。

## 2026-07-20：AI 复盘页面高度收口

- 修复 AI 复盘页出现外层页面滚动条的问题：复盘视图通过 `content-review` 接管内容区高度，工作台填满内容视口，不再把固定 `100vh` 与内容区自身滚动叠加。
- 保留窄屏布局的自然滚动行为；桌面宽屏下报告 / 顾问区域继续在内部面板滚动，避免整页被撑高。
- 新增 `test_ai_replay_workspace_fits_content_viewport_without_page_scrollbar`，前端契约测试共 `30 passed`；未打安装包。
