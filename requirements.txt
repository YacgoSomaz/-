# 音频链路（已验证可用）
playwright>=1.40.0       # 拉流地址获取（拦截网络流量）
imageio-ffmpeg>=0.5.1    # 自带 ffmpeg 二进制，免手动配 PATH
openai-whisper>=20231117 # 本地 CPU 转写
numpy>=1.24.0            # WAV 解码喂给 Whisper
opencc-python-reimplemented>=0.1.7 # 繁体转简体后处理
aiosqlite>=0.19.0        # 异步落库

# 评论链路（Playwright 拦截方案）
protobuf>=4.25.0         # 解析弹幕 protobuf
